# LessonFlowAI 环境配置示例
# 复制此文件为 .env 并填入实际值

# ============ 阿里云 TTS 配置 ============
# 阿里云 AccessKey ID (必需)
# 获取方式: https://ram.console.aliyun.com/manage/ak
ALIYUN_ACCESS_KEY_ID=your_access_key_id

# 阿里云 AccessKey Secret (必需)
# 获取方式: 同上，创建 AccessKey 时显示
ALIYUN_ACCESS_KEY_SECRET=your_access_key_secret

# 阿里云智能语音交互 AppKey (必需，用于语音合成)
# 获取方式: https://nls-portal.console.aliyun.com/applist
# 注意: Token 会使用 AK/SK 自动刷新，但语音合成仍需要 AppKey
ALIYUN_TTS_APP_KEY=your_app_key

# ============ TTS 可选配置 ============
# 默认语音角色 (可选值: zhitian_emo, zhichu_emo, zhide_emo 等)
ALIYUN_TTS_VOICE=zhitian_emo

# 默认语速 (-500 到 500，0 为正常语速)
ALIYUN_TTS_SPEECH_RATE=0

# ============ Manim 配置 ============
# 默认渲染质量 (l=低, m=中, h=高, k=4K)
MANIM_QUALITY=h

# 渲染输出目录 (相对于课程目录)
MANIM_OUTPUT_DIR=renders

# ============ 视频合成配置 ============
# FFmpeg 路径 (如果不在 PATH 中)
# FFMPEG_PATH=/usr/local/bin/ffmpeg

# 默认视频编码器
VIDEO_ENCODER=libx264

# 默认 CRF 值 (18-28，越小质量越高)
VIDEO_CRF=18

# ============ 其他配置 ============
# 日志级别 (DEBUG, INFO, WARNING, ERROR)
LOG_LEVEL=INFO

# 是否在渲染时显示网格（调试用）
DEBUG_SHOW_GRID=false
