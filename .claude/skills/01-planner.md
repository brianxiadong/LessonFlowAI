---
name: lesson-planner
description: 根据用户的自然语言描述，生成教学课程大纲和分镜脚本
---

# 课程策划 Skill (Planner)

## 概述

此 Skill 负责将用户的自然语言需求转换为结构化的教学大纲和分镜脚本。这是整个 LessonFlowAI 流水线的第一步，输出将作为后续所有 Skill 的输入基础。

## 触发条件

当用户说出类似以下内容时触发：
- "生成一个讲解 XXX 的教学视频"
- "帮我做一个关于 XXX 的动画课程"
- "创建 XXX 主题的教学内容，时长 X 分钟"
- "lesson plan: XXX"

## 输入参数

用户应提供以下信息（可从自然语言中提取）：

| 参数 | 必需 | 说明 | 默认值 |
|------|------|------|--------|
| topic | **是** | 教学主题 | - |
| duration | 否 | 目标时长（秒） | 180 |
| audience | 否 | 目标受众 | beginner |
| language | 否 | 语言 | zh-CN |
| style | 否 | 视觉风格 | tech-minimal |

**示例输入**：
```
讲 Transformer Attention，3分钟，面向初学者，风格简洁科技感
```

**解析结果**：
```json
{
  "topic": "Transformer Attention",
  "duration": 180,
  "audience": "beginner",
  "language": "zh-CN",
  "style": "tech-minimal"
}
```

## 执行步骤

### 步骤 1：理解教学主题

分析用户提供的主题，确定：
1. 核心概念有哪些
2. 需要哪些前置知识
3. 关键术语列表
4. 适合的可视化方式

### 步骤 2：生成教学大纲 (outline.md)

根据时长和受众，规划教学结构：

```markdown
# [课程标题]

## 课程信息
- 主题: [topic]
- 时长: [duration]秒
- 受众: [audience]
- 风格: [style]

## 学习目标
1. [目标1]
2. [目标2]
3. [目标3]

## 内容大纲

### 1. 引入 (约X秒)
- 引出问题/场景
- 建立学习动机

### 2. 核心概念 (约X秒)
- 概念A解释
- 概念B解释

### 3. 示例演示 (约X秒)
- 具体例子
- 可视化展示

### 4. 总结回顾 (约X秒)
- 要点回顾
- 延伸思考
```

### 步骤 3：生成术语表 (glossary.json)

提取所有专业术语，配置正确发音：

```json
{
  "meta": {
    "domain": "[领域]",
    "language": "zh-CN"
  },
  "terms": {
    "Attention": {
      "pronunciation": {
        "ipa": "əˈtenʃən"
      },
      "alias": "注意力机制",
      "definition": "一种让模型关注输入中重要部分的机制"
    },
    "Query": {
      "pronunciation": {
        "ipa": "ˈkwɪri"
      },
      "alias": "查询向量"
    }
  }
}
```

### 步骤 4：生成分镜脚本 (storyboard.json)

**核心约束**：
- 每个 Scene 时长 5-15 秒
- 每个 Scene 元素数量 ≤ 12
- 所有元素必须使用网格锚点定位
- 镜头变化明确，动画动作具体

**生成规则**：
1. 将大纲拆分为多个 Scene
2. 每个 Scene 对应一个教学点
3. 确保动画步骤与旁白时长匹配
4. 为每个 Scene 计算内容 hash（用于增量更新）

**分镜脚本结构**：

```json
{
  "meta": {
    "title": "[课程标题]",
    "duration_target_s": [总时长],
    "audience": "[受众]",
    "language": "zh-CN",
    "style": "[风格]",
    "version": "1.0.0"
  },
  "scenes": [
    {
      "id": "scene_001",
      "duration_s": 10,
      "_hash": "[内容hash]",
      "visual": {
        "elements": [
          {
            "type": "text",
            "id": "title",
            "content": "什么是 Attention?",
            "anchor": "top-center",
            "size": "large"
          }
        ],
        "layout": {
          "grid": "3x3",
          "margin": 0.5
        }
      },
      "animation": {
        "steps": [
          {
            "action": "write",
            "target": "title",
            "duration_s": 2
          }
        ]
      },
      "narration": {
        "vo_text": "今天我们来学习 Attention 机制。",
        "voice": "zhitian_emo",
        "speed": 1,
        "pause_after_s": 0.5
      },
      "subtitle": {
        "text": "今天我们来学习 Attention 机制"
      },
      "checks": {
        "must_show": ["title"],
        "no_overlap": true,
        "bounds_check": true
      }
    }
  ]
}
```

### 步骤 5：验证分镜脚本

运行 Schema 验证：

```bash
python scripts/validate_storyboard.py courses/[lesson_id]/storyboard.json
```

检查项：
- [ ] 所有必填字段完整
- [ ] Scene 时长在 5-15 秒范围
- [ ] 每个 Scene 元素数 ≤ 12
- [ ] 锚点值合法
- [ ] 动画动作类型合法
- [ ] 总时长接近目标时长（±10%）

## 输出产物

执行完成后，在 `courses/[lesson_id]/` 目录生成：

```
courses/
  lesson_001/
    outline.md          # 教学大纲
    storyboard.json     # 分镜脚本（核心）
    glossary.json       # 术语表
```

## 元素类型参考

| type | 说明 | 必需属性 |
|------|------|----------|
| text | 文本 | content, anchor |
| formula | LaTeX公式 | content, anchor |
| box | 带标签方框 | label, anchor, color |
| circle | 圆形 | anchor, color |
| arrow | 箭头 | from, to |
| image | 图片 | src, anchor |
| axes | 坐标轴 | anchor |
| graph | 函数图像 | formula, anchor |

## 动画动作参考

| action | 说明 | 适用场景 |
|--------|------|----------|
| create | 创建/绘制 | 几何图形 |
| write | 书写 | 文本、公式 |
| fade_in | 淡入 | 任意元素 |
| fade_out | 淡出 | 任意元素 |
| transform | 变换 | 形状变换 |
| move_to | 移动 | 位置改变 |
| scale | 缩放 | 强调 |
| highlight | 高亮 | 重点强调 |
| indicate | 指示 | 临时强调 |
| wait | 等待 | 停顿 |

## 可视化风格对照

| style | 配色 | 适用场景 |
|-------|------|----------|
| tech-minimal | 深蓝背景+亮色元素 | 技术/编程 |
| hand-drawn | 米色背景+手绘风 | 数学/物理 |
| corporate | 白色背景+商务蓝 | 商业/管理 |
| playful | 暖色背景+活泼配色 | 儿童/入门 |
| academic | 浅灰背景+学术风 | 论文解读 |

## 示例：完整执行流程

**用户输入**：
```
生成一个讲解 Transformer Self-Attention 的教学视频，3分钟，面向初学者
```

**执行**：

1. 解析参数：
   - topic: "Transformer Self-Attention"
   - duration: 180
   - audience: "beginner"
   - style: "tech-minimal"（默认）

2. 创建课程目录：
   ```bash
   mkdir -p courses/lesson_transformer_attention
   ```

3. 生成大纲 → outline.md
4. 生成术语表 → glossary.json
5. 生成分镜脚本 → storyboard.json
6. 运行验证脚本

**输出确认**：
```
✅ 课程策划完成！
📁 课程目录: courses/lesson_transformer_attention/
📄 大纲: outline.md (5个章节)
📄 分镜: storyboard.json (12个场景)
📄 术语表: glossary.json (8个术语)
⏱️ 预计时长: 175秒

下一步: 运行 Skill 2 (Animator) 生成 Manim 代码
```

## 增量更新支持

当用户说"修改第3个场景的标题"时：
1. 读取现有 storyboard.json
2. 仅修改 scene_003
3. 重新计算 scene_003._hash
4. 保存更新后的文件

其他未变更的 Scene 保持原有 _hash，后续 Skill 可跳过重新处理。
