---
name: lesson-flow
description: 主控编排 Skill，串联 6 个子 Skill 完成端到端教学视频生成
---

# 主控编排 Skill (Orchestrator)

## 概述

此 Skill 是 LessonFlowAI 的主入口，负责：
- 解析用户的自然语言需求
- 串联调用 6 个子 Skill
- 管理增量更新和缓存
- 错误恢复和回滚
- 产物校验和输出

## 触发条件

当用户输入以下类似内容时触发：

```
生成一个讲解 Transformer Attention 的教学视频，3分钟，面向初学者
```

```
lesson: 傅里叶变换，5分钟，大学生，学术风格
```

```
创建课程：机器学习梯度下降，2分钟
```

## 完整流水线

```
┌─────────────────────────────────────────────────────────────────────────┐
│                        LessonFlowAI 流水线                               │
├─────────────────────────────────────────────────────────────────────────┤
│                                                                         │
│  用户输入                                                                │
│  "讲 Transformer Attention，3分钟，面向初学者，风格简洁科技感"            │
│                                                                         │
│       │                                                                 │
│       ▼                                                                 │
│  ┌─────────────────┐                                                    │
│  │ Skill 1        │  输出:                                              │
│  │ Planner        │  ├── outline.md                                     │
│  │ 课程策划        │  ├── storyboard.json                                │
│  └────────┬────────┘  └── glossary.json                                 │
│           │                                                             │
│           ▼                                                             │
│  ┌─────────────────┐                                                    │
│  │ Skill 2        │  输出:                                              │
│  │ Animator       │  └── scenes/scene_*.py                              │
│  │ 代码生成        │                                                     │
│  └────────┬────────┘                                                    │
│           │                                                             │
│           ▼                                                             │
│  ┌─────────────────┐                                                    │
│  │ Skill 3        │  输出:                                              │
│  │ Builder        │  ├── renders/scene_*.mp4                            │
│  │ 编译渲染        │  └── logs/qa_report.json                            │
│  └────────┬────────┘                                                    │
│           │                                                             │
│           ▼                                                             │
│  ┌─────────────────┐                                                    │
│  │ Skill 5        │  输出:                                              │
│  │ Voice          │  ├── audio/scene_*.wav                              │
│  │ 配音生成        │  └── audio/scene_*_timestamps.json                  │
│  └────────┬────────┘                                                    │
│           │                                                             │
│           ▼                                                             │
│  ┌─────────────────┐                                                    │
│  │ Skill 4        │  输出:                                              │
│  │ Subtitles      │  ├── subs/scene_*.srt                               │
│  │ 字幕生成        │  └── subs/full_lesson.srt                           │
│  └────────┬────────┘                                                    │
│           │                                                             │
│           ▼                                                             │
│  ┌─────────────────┐                                                    │
│  │ Skill 6        │  输出:                                              │
│  │ Post           │  ├── final/lesson_1080p.mp4                         │
│  │ 合成出片        │  ├── final/lesson_1080p_subs.mp4                    │
│  └────────┬────────┘  └── final/thumbnail.jpg                           │
│           │                                                             │
│           ▼                                                             │
│  ┌─────────────────┐                                                    │
│  │ 完成！          │                                                    │
│  │ 产物打包        │                                                    │
│  └─────────────────┘                                                    │
│                                                                         │
└─────────────────────────────────────────────────────────────────────────┘
```

## 执行步骤

### 步骤 0：解析用户输入

从自然语言中提取参数：

```python
def parse_user_input(user_input: str) -> dict:
    """
    解析用户输入，提取课程参数
    """
    # 示例解析逻辑
    params = {
        "topic": None,
        "duration": 180,  # 默认 3 分钟
        "audience": "beginner",
        "language": "zh-CN",
        "style": "tech-minimal"
    }
    
    # 提取主题（通常在"讲"、"关于"、"主题"后面）
    topic_patterns = [
        r"讲(?:解)?[「『]?(.+?)[」』]?[，,]",
        r"关于[「『]?(.+?)[」』]?的",
        r"主题[：:]?\s*(.+?)[，,]",
    ]
    
    # 提取时长
    duration_pattern = r"(\d+)\s*分钟"
    
    # 提取受众
    audience_map = {
        "初学者": "beginner",
        "入门": "beginner",
        "进阶": "intermediate",
        "高级": "advanced",
        "通用": "general",
    }
    
    # 提取风格
    style_map = {
        "科技感": "tech-minimal",
        "简洁": "tech-minimal",
        "手绘": "hand-drawn",
        "商务": "corporate",
        "活泼": "playful",
        "学术": "academic",
    }
    
    return params
```

### 步骤 1：初始化课程目录

```bash
# 生成课程 ID
LESSON_ID="lesson_$(date +%Y%m%d_%H%M%S)"

# 创建目录结构
mkdir -p courses/$LESSON_ID/{scenes,renders,audio,subs,final,logs}
```

### 步骤 2：执行 Skill 1 (Planner)

```
调用 Skill: lesson-planner
输入: 解析后的参数
输出: outline.md, storyboard.json, glossary.json
```

**检查点**：
- [ ] storyboard.json 通过 Schema 验证
- [ ] 场景总时长在目标时长 ±10%
- [ ] glossary.json 包含所有专业术语

### 步骤 3：执行 Skill 2 (Animator)

```
调用 Skill: manim-animator
输入: storyboard.json, style_guide.json
输出: scenes/scene_*.py
```

**检查点**：
- [ ] 所有 Python 文件语法正确
- [ ] 每个 Scene 类可被导入

### 步骤 4：执行 Skill 3 (Builder)

```
调用 Skill: manim-builder
输入: scenes/*.py
输出: renders/*.mp4, qa_report.json
```

**检查点**：
- [ ] 所有场景渲染成功
- [ ] 无严重质量问题（边界越界）

**失败处理**：
- 如果某个 Scene 渲染失败，记录错误
- 尝试自动修复（最多 3 次）
- 3 次失败后，跳过该 Scene 或暂停流水线

### 步骤 5：执行 Skill 5 (Voice)

```
调用 Skill: tts-voice
输入: storyboard.json (narration), glossary.json
输出: audio/*.wav, audio/*_timestamps.json
```

**检查点**：
- [ ] 所有场景有对应音频
- [ ] 时间戳文件存在

**费用确认**：
```
📊 TTS 费用预估: ¥0.57
确认生成配音? (y/n)
```

### 步骤 6：执行 Skill 4 (Subtitles)

```
调用 Skill: subtitle-generator
输入: audio/*_timestamps.json
输出: subs/*.srt, subs/full_lesson.srt
```

**检查点**：
- [ ] 字幕时间轴无重叠
- [ ] 字幕与音频时长匹配

### 步骤 7：执行 Skill 6 (Post)

```
调用 Skill: video-post
输入: renders/*.mp4, audio/*.wav, subs/*.srt
输出: final/*.mp4
```

**检查点**：
- [ ] 最终视频可正常播放
- [ ] 音视频同步
- [ ] 字幕正确显示

### 步骤 8：产物打包与报告

生成最终报告：

```markdown
# 课程生成报告

## 基本信息
- 课程ID: lesson_20260129_103000
- 主题: Transformer Attention
- 时长: 3分12秒
- 生成时间: 2026-01-29 10:35:00

## 产物清单
| 文件 | 路径 | 大小 |
|------|------|------|
| 大纲 | outline.md | 2KB |
| 分镜脚本 | storyboard.json | 15KB |
| 术语表 | glossary.json | 3KB |
| Manim 代码 | scenes/ | 25KB |
| 渲染视频 | renders/ | 120MB |
| 配音 | audio/ | 8MB |
| 字幕 | subs/ | 5KB |
| 最终视频 | final/lesson_1080p.mp4 | 45MB |
| 字幕版 | final/lesson_1080p_subs.mp4 | 46MB |

## 费用统计
- TTS 费用: ¥0.57
- 总计: ¥0.57

## 复现命令
```bash
cd courses/lesson_20260129_103000
lessonflow rebuild
```
```

## 增量更新流程

当用户说 "修改第 2 个场景的标题为 XXX"：

```
┌─────────────────────────────────────────────────────────────────┐
│                      增量更新流程                                │
├─────────────────────────────────────────────────────────────────┤
│  1. 定位修改目标                                                 │
│     └─► scene_002                                               │
│                                                                 │
│  2. 更新 storyboard.json                                        │
│     └─► 修改 scene_002 的 visual.elements                       │
│     └─► 重新计算 scene_002._hash                                │
│                                                                 │
│  3. 对比 .build_cache.json                                      │
│     └─► scene_001: hash 未变 → 跳过                             │
│     └─► scene_002: hash 已变 → 需要重建                          │
│     └─► scene_003: hash 未变 → 跳过                             │
│                                                                 │
│  4. 仅重新执行受影响的步骤                                        │
│     ├─► Skill 2: 重新生成 scene_002.py                          │
│     ├─► Skill 3: 重新渲染 scene_002.mp4                         │
│     ├─► Skill 5: 跳过（narration 未变）                          │
│     ├─► Skill 4: 跳过（时间戳未变）                               │
│     └─► Skill 6: 重新合成（因为视频变了）                         │
│                                                                 │
│  5. 更新 .build_cache.json                                      │
└─────────────────────────────────────────────────────────────────┘
```

## 错误恢复策略

```python
class LessonFlowOrchestrator:
    def run(self, user_input: str):
        lesson_id = self.init_lesson()
        
        try:
            # 步骤 1: Planner
            self.run_skill("planner", lesson_id)
            self.checkpoint("planner_done")
            
            # 步骤 2: Animator
            self.run_skill("animator", lesson_id)
            self.checkpoint("animator_done")
            
            # 步骤 3: Builder
            self.run_skill("builder", lesson_id)
            self.checkpoint("builder_done")
            
            # 步骤 4: Voice
            self.run_skill("voice", lesson_id)
            self.checkpoint("voice_done")
            
            # 步骤 5: Subtitles
            self.run_skill("subtitles", lesson_id)
            self.checkpoint("subtitles_done")
            
            # 步骤 6: Post
            self.run_skill("post", lesson_id)
            self.checkpoint("complete")
            
        except SkillError as e:
            # 记录失败点
            self.log_error(e)
            
            # 提示用户
            print(f"❌ 流水线在 {e.skill_name} 步骤失败")
            print(f"错误: {e.message}")
            print(f"可以运行 'lessonflow resume {lesson_id}' 从失败点继续")
    
    def resume(self, lesson_id: str):
        """从上次失败点继续"""
        last_checkpoint = self.get_last_checkpoint(lesson_id)
        
        # 从下一个步骤继续
        remaining_skills = self.get_remaining_skills(last_checkpoint)
        for skill in remaining_skills:
            self.run_skill(skill, lesson_id)
```

## 使用示例

### 示例 1：完整生成

**用户**：
```
生成一个讲解 Transformer Self-Attention 的教学视频，3分钟，面向初学者，简洁科技风格
```

**执行流程**：
```
🚀 开始生成课程...
📋 课程ID: lesson_20260129_103000
📝 主题: Transformer Self-Attention
⏱️ 目标时长: 180秒
👥 受众: 初学者
🎨 风格: tech-minimal

[1/6] 📝 课程策划 (Planner)...
   ✅ 生成大纲: 5 个章节
   ✅ 生成分镜: 12 个场景
   ✅ 生成术语表: 8 个术语

[2/6] 💻 代码生成 (Animator)...
   ✅ scene_001.py
   ✅ scene_002.py
   ...
   ✅ scene_012.py

[3/6] 🎬 渲染动画 (Builder)...
   ⏳ 渲染中... (预计 5 分钟)
   ✅ 12/12 场景渲染完成

[4/6] 🎙️ 生成配音 (Voice)...
   📊 费用预估: ¥0.57
   ✅ 配音生成完成

[5/6] 📝 生成字幕 (Subtitles)...
   ✅ 32 条字幕，已对齐

[6/6] 🎞️ 合成视频 (Post)...
   ✅ lesson_1080p.mp4 (45MB)
   ✅ lesson_1080p_subs.mp4 (46MB)

🎉 课程生成完成！
📁 输出目录: courses/lesson_20260129_103000/final/
📹 主视频: lesson_1080p.mp4
💰 总费用: ¥0.57
```

### 示例 2：增量修改

**用户**：
```
把第 3 个场景的 Query 方框改成红色
```

**执行流程**：
```
🔄 增量更新模式
📍 修改目标: scene_003

[1/6] 📝 更新分镜脚本...
   ✅ scene_003 已更新

[2/6] 💻 重新生成代码...
   ⏭️ scene_001.py (跳过)
   ⏭️ scene_002.py (跳过)
   🔄 scene_003.py (重新生成)
   ⏭️ scene_004.py (跳过)
   ...

[3/6] 🎬 重新渲染...
   ⏭️ scene_001.mp4 (跳过)
   ⏭️ scene_002.mp4 (跳过)
   🔄 scene_003.mp4 (重新渲染)
   ⏭️ scene_004.mp4 (跳过)
   ...

[4/6] 🎙️ 配音...
   ⏭️ 全部跳过 (旁白未变更)

[5/6] 📝 字幕...
   ⏭️ 全部跳过 (时间戳未变更)

[6/6] 🎞️ 重新合成...
   🔄 合成最终视频

✅ 增量更新完成！
   重新生成: 1 个场景
   节省时间: 约 4 分钟
```

## 产物目录结构

```
courses/lesson_20260129_103000/
├── outline.md                    # 教学大纲
├── storyboard.json               # 分镜脚本（核心 DSL）
├── glossary.json                 # 术语表
├── style_guide.json              # 风格配置
├── .build_cache.json             # 构建缓存（增量更新用）
│
├── scenes/                       # Manim 源码
│   ├── __init__.py
│   ├── scene_001.py
│   ├── scene_002.py
│   └── ...
│
├── renders/                      # 渲染视频
│   ├── scene_001.mp4
│   ├── scene_002.mp4
│   └── ...
│
├── audio/                        # 配音文件
│   ├── scene_001.wav
│   ├── scene_001_timestamps.json
│   └── ...
│
├── subs/                         # 字幕文件
│   ├── scene_001.srt
│   ├── full_lesson.srt
│   └── full_lesson.vtt
│
├── final/                        # 最终产物
│   ├── lesson_1080p.mp4
│   ├── lesson_1080p_subs.mp4
│   ├── lesson_1080p_soft.mkv
│   └── thumbnail.jpg
│
└── logs/                         # 日志
    ├── build.log
    ├── qa_report.json
    └── tts_cost.log
```

## 快捷命令

| 命令 | 说明 |
|------|------|
| `lesson: <描述>` | 完整生成新课程 |
| `修改第X个场景...` | 增量更新指定场景 |
| `重新渲染` | 重新执行 Builder |
| `重新配音` | 重新执行 Voice |
| `预览场景 X` | 快速预览单个场景 |
| `导出 720p` | 生成其他分辨率版本 |
